<div class="contents row">
  <% @subjects.each do |subject| %>
    <%= image_tag subject.image, class: 'message-image' if subject.image.attached? %>
      <p><%= subject.subject_name %></p>
      <span class="name">
        <%= user.nickname %>
      </span>
    </div>
  <% end %>
</div>